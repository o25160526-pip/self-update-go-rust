# .env.example — Tauri signing keys
# Generate with: npx tauri signer generate -w ~/.tauri/rust-demo.key
# Copy private key CONTENT (not path) here for local testing.
# NEVER commit the real .env file — it's in .gitignore.

TAURI_SIGNING_PRIVATE_KEY=
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
